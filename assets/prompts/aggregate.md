Aggregate these datasets and extract project-level patterns without citing or referencing specific details. The identified patterns must be mutually exclusive with no conceptual overlaps. Output the result as a numbered list where each item includes a clear pattern name and its corresponding description.

Each dataset item carries a `source` field (`user` or `log`). Track provenance while aggregating: if a pattern is derived from at least one item whose `source` is `user`, prefix that pattern's line with the marker `[user] ` (before the pattern name). Patterns derived only from `log` items get no prefix. This marker is a provenance class, not a specific detail — it does not violate the "no citations" rule. Format: `N. [user] Pattern name — description` for user-derived patterns, `N. Pattern name — description` otherwise.
